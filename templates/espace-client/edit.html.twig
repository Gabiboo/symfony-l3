{% extends '/espace-client/layout.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-md-12">
            <div class="full">
                <div class="heading_main text_align_center">
                    <h2><span class="theme_color text-align-center"></span>Modifier mes informations</h2>
                </div>
            </div>
        </div>
    </div>
    <a class="btn btn-light" href="{{ path('user_home') }}"> < Mon espace</a>

    {{ include('user/_form.html.twig', {'button_label': 'Update'}) }}

    <div class="text_align_right"> 
        <h2>Attention, supprimer son compte est une action irrevérsible.</h2>
        {{ include('user/_delete_form.html.twig') }}
    </div>
    
{% endblock %}
