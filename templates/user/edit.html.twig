{% extends '/admin/layout.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-md-12">
            <div class="full">
                <div class="heading_main text_align_center">
                    <h2><span class="theme_color text-align-center"></span>Éditer {{ user.email }} :</h2>
                </div>
            </div>
        </div>
    </div>

    <div>
        <div class="col-4">
            <a class="btn btn-light" href="{{ path('user_index') }}"> < revenir à la liste</a>
        </div>
        <div class="col-12 text_align_center">
            {{ include('user/_form.html.twig', {'button_label': 'Update'}) }}
            
        </div>
        <div class="col-12 text_align_right">
            <p>Attention, supprimer un compte est une action irrevérsible</p>
            {{ include('user/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
