{% extends '/admin/layout.html.twig' %}

{% block title %}New Offer{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-md-12">
            <div class="full">
                <div class="heading_main text_align_center">
                    <h2><span class="theme_color text-align-center"></span>Créer une nouvelle offre</h2>
                </div>
            </div>
        </div>
    </div>

    {{ include('offer/_form.html.twig') }}

    <a class="btn btn-light" href="{{ path('offer_index') }}"> < liste des offres</a>
{% endblock %}
