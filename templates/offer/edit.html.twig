{% extends '/admin/layout.html.twig' %}

{% block title %}Edit Offer{% endblock %}

{% block body %}
    <div class="row">
        <div class="col-md-12">
            <div class="full">
                <div class="heading_main text_align_center">
                    <h2><span class="theme_color text-align-center"></span>Éditer l'offre {{ offer.titre }}</h2>
                </div>
            </div>
        </div>
    </div>

    
    {{ include('offer/_form.html.twig', {'button_label': 'Update'}) }}

    

    

    <div class="row">
        <div class="col-6 text_align_left">
            <a class="btn btn-light" href="{{ path('offer_index_a') }}"> < liste des offres</a>
        </div>
        
        <div class="col-6 text_align_right">
            <p class=" alert-danger">Attention : Supprimer une offre est une action irrevérsible.</p>
            {{ include('offer/_delete_form.html.twig') }}
        </div>
    </div>

{% endblock %}
